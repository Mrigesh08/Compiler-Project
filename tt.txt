_main[]
  int a , c;
  a=5;
  matrix b;
  b=[1, 2, 3, 4,4,6,8,5,3 ; 4 , 4, 5, 7];
  # the above line is giving problems in parsing, leading to assertion error
  function [string s]= _concatenate[string a, string a2]
  	s = a + a2;
  end;


  read(x);
  print(a);

  x= (5 / 5)+6;

  y=a[2,3];

  z= _fjdsa(a,b);

end
